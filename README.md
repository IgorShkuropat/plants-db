# plants-db

It's simple CRUD web-app on python Django!

Dependencies:
after installin pip and virtual environments enter this command:
python -m pip install Django

To run app enter command:
py manage.py runserver
